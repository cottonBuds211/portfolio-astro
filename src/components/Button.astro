---
const { title, type, link, target } = Astro.props;
const defaultStyle =
  "flex items-center py-2 px-4 flex gap-2 cursor-pointer border-1 transition-all ease-in duration-200 border-black/25 w-fit rounded-sm  hover:backdrop-blur-2xl hover:shadow-lg hover:scale-105";

 const primary = "bg-secondary text-default-bg " 

---

<a
  class={`${defaultStyle} ${type === "primary" ? primary : ""}`}
  href={link}
  target={target}
>
  {title}
  {title === "Resumé" ? <svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="lucide lucide-arrow-right-icon lucide-arrow-right"><path d="M5 12h14"/><path d="m12 5 7 7-7 7"/></svg> : ""}
</a>